import { ApiEndpoint } from "@/components/mdx/ApiEndpoint";
import { DocsPagination } from "@/components/mdx/DocsPagination";
import { DocHero } from "@/components/mdx/DocHero";

<DocHero 
  title="Agent API" 
  subtitle="The Agent API is how your AI interfaces with the OUAS system to request manifests, submit layouts, and receive validation feedback." 
/>

## Endpoints

<ApiEndpoint 
  method="GET" 
  path="/ouas/manifest" 
  description="Returns the full Component Manifest for the current application context. The agent uses this to understand which components are available and what props they accept."
  response={`{
  "ouas_version": "1.0",
  "app_id": "mailflow-app",
  "components": [ ... ]
}`}
/>

<ApiEndpoint 
  method="POST" 
  path="/ouas/transform" 
  description="Submits a Layout Config generated by the agent. The server will validate this config against the manifest before applying it."
  response={`{
  "status": "success",
  "applied_layout": "layout-uuid-1234",
  "warnings": []
}`}
/>

<ApiEndpoint 
  method="POST" 
  path="/ouas/validate" 
  description="Dry-run validation for a proposed Layout Config without actually applying it to the application state."
  response={`{
  "valid": false,
  "errors": [
    { "code": "E_MISSING_PROP", "message": "Component 'UserCard' requires prop 'userId'" }
  ]
}`}
/>

<DocsPagination 
  prev={{ title: "Validation Pipeline", href: "/docs/validation", description: "Learn how layouts are safely validated" }}
  next={{ title: "Agent Skills", href: "/docs/skills", description: "Teach agents the OUAS programming model" }}
/>
